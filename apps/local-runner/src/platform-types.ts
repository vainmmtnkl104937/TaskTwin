import type { RunnerDeviceMetadata } from '@tasktwin/runner-protocol';

export type RunnerPlatform = RunnerDeviceMetadata['platform'];
export type RunnerArchitecture = RunnerDeviceMetadata['architecture'];
