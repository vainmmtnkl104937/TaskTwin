# @tasktwin/workflow-approval

Framework-independent deterministic contracts for TaskTwin human approval
gates. An Approval Step gates only its immediate following workflow step.

The package contains no HTTP, UI, database, browser, filesystem, Playwright or
framework behavior. Safe summaries intentionally omit approval messages,
runtime values, secrets, outputs, locators, URLs and user identity.
