# Workflow extraction

Framework-independent contracts and deterministic producer/consumer analysis
for ephemeral workflow outputs. Runtime values are deliberately excluded from
all public schemas and safe summaries.
